# ローカル版と本番版（App Store配信版）の違い

**方針：ローカル版は本番版の強化版。** 同じコード・同じ画面・同じ機能で、
ローカルだけ「配信物には入れられないもの」が上乗せされている。

分岐はすべて **`ANTHROPIC_API_KEY` の有無** で決まる。
- **無い＝ローカル版**：`claude.exe`（Claude Max枠）を起動する。**1円も掛からない**。
- **有る＝本番版**：Anthropic API を従量課金で叩く。

> ⚠️ だから `ANTHROPIC_API_KEY` をローカルの `.env.local` に入れてはいけない。
> 入れた瞬間にローカルまで課金モードになる。

---

## 一覧

| | ローカル版（大翔が自分で使う） | 本番版（App Store / Vercel） |
|---|---|---|
| **AIモデル** | **Opus**（`--model opus`） | レシピ探索・取り込み＝**Sonnet 5**<br>写真の食材認識・テキスト＝**Haiku 4.5** |
| **AI利用料** | **¥0**（Claude Max枠） | 従量課金（research ¥19.6 / scan ¥0.53 / import ¥12.8 per回） |
| **回数の上限** | **なし**（`quotaEnforced()` が false） | あり（無料 8/25/5・プレミアム 60/300/20） |
| **動画からレシピ** | 概要欄 ＋ **字幕**（yt-dlp） | 概要欄のみ |
| **AI画像生成** | あり | なし（絵文字表示） |
| **データの保存先** | `.data/*.json`（この PC の中） | Upstash Redis |
| **Googleログイン** | 未設定なら出ない | あり |

---

## 唯一の「機能差」＝動画取り込みの字幕

大翔の認識どおり、**機能として弱いのはここだけ**。

- **ローカル**：`src/lib/videoLocal.ts` が yt-dlp を起動し、概要欄に加えて**字幕**まで取る。
- **本番**：`src/lib/videoMeta.ts` が素のHTTPで oEmbed＋ページHTMLを読み、**概要欄まで**。

### なぜ本番で字幕を取らないのか

素の fetch では timedtext が **0バイト**で返る（実測）。取るには yt-dlp 相当の
署名トークン生成が必要で、それは **YouTube の私的APIを叩く行為＝利用規約違反**。
App Store で配信するものに規約違反は入れられないので、本番では入れない。
ローカルは大翔の私用なので入れる。

### 誤解しやすい点

**「SNSを調べる機能」は本番でも生きている。** 字幕が取れないだけで、
レシピ探索（`/api/research`）は Web検索で YouTube・TikTok・Instagram の
人気レシピを見つけて `sources` に出典として載せる。ここは両者で同じ。

---

## 実務上の差

| | ローカル | 本番 |
|---|---|---|
| レシピの精度 | Opus＋字幕で最も高い | 概要欄ベース。概要欄に材料が書いてあれば遜色ない |
| 写真の食材認識 | Opus | Haiku 4.5（速い・安い） |
| 何回でも使えるか | 使える | 無料枠あり |

---

## 起動

```bash
# ローカル版（強化版）
npm run dev -- -p 3014     # → http://localhost:3014
```

`.env.local` に `ANTHROPIC_API_KEY` を**書かないこと**。書くと本番と同じ挙動になる。
